# Handbook Assets

Real device/simulator screenshots go here as screens get visually settled — captured fresh, not pulled from elsewhere. Reference from `../00-overview.md` (or a new numbered handbook page) with a relative markdown image link once added.

Illustration/hero art already committed under `apps/mobile/assets/` is referenced directly from handbook pages by relative path rather than copied here — one copy of each image, not two.
