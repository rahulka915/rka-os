# Approved logo refinement references

These four crops isolate the approved negative-space directions from the wider exploration board:

- `a3.png` — clearest spacing and balance
- `f2.png` — strongest inward convergence
- `f3.png` — preferred structural base
- `f4.png` — cleanest geometric cuts

Use all four as generation references. The essential invariant is that `RKA` is the dark negative space created between eight surrounding light contribution blocks. Do not typeset, overlay, emboss or draw separate letter glyphs across the centre.

